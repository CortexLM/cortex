# Owner-signed measurement allowlist retained by the frozen bundle contract.
# The Python Firecracker path does not authorize a legacy CVM measurement, so
# the list is intentionally empty. Additions require the offline owner ceremony.
version = 5
introduced_epoch = 0

measurements = []
