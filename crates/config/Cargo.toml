[package]
name = "config"
description = "Typed layered configuration: defaults → base.toml → BASE_* (CORTEX_* aliases)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"
toml = "0.8"

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
