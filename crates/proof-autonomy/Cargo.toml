[package]
name = "proof-autonomy"
description = "Proof consent, experiment lifecycle and Atlas decision contracts"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
bundle = { path = "../bundle" }
challenge-common = { path = "../challenge-common" }
crypto = { path = "../crypto" }
hex = "0.4"
proof-task = { path = "../proof-task" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
thiserror = "2"
uuid = { version = "1", features = ["v4", "serde"] }

[dev-dependencies]
aggregate = { path = "../aggregate" }

[lints]
workspace = true
