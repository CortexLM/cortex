[package]
name = "proof-results"
description = "Obligatory topic-defined RLM results JSON: contract ids, shape check, scored-fact bind. Display/audit — never a second score."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
proof-canon = { path = "../proof-canon" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[lints]
workspace = true
