[package]
name = "attest-http"
description = "Validator attestation HTTP: nonce issue + quote submit (task 38)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
publish = false

[features]
default = []
# Real Intel DCAP verification (attest-policy::DcapQuoteVerifier).
dcap = ["attest-policy/dcap"]

[dependencies]
axum = { version = "0.8", default-features = false, features = ["http1", "tokio", "json"] }
attest-policy = { path = "../attest-policy" }
chain = { path = "../chain" }
crypto = { path = "../crypto" }
db = { path = "../db" }
trustroot = { path = "../trustroot" }
serde = { version = "1", features = ["derive"] }
hex = "0.4"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "sync"] }
tracing = "0.1"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }

[lints]
workspace = true
