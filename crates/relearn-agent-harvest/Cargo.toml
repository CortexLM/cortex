[package]
name = "relearn-agent-harvest"
description = "Relearn Agent live harvest: digest-pinned eval image on a Lium pod"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
async-trait = "0.1"
harvest-pod = { path = "../harvest-pod" }
prism-lium-types = { path = "../prism-lium-types" }
relearn-agent-eval = { path = "../relearn-agent-eval" }
relearn-agent-score = { path = "../relearn-agent-score" }
relearn-agent-task = { path = "../relearn-agent-task" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tracing = "0.1"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt", "rt-multi-thread"] }

[lints]
workspace = true
