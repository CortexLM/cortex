[package]
name = "proof-harvest"
description = "Proof live harvest: digest-pinned proof-eval image on a Lium pod"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
async-trait = "0.1"
harvest-pod = { path = "../harvest-pod" }
prism-lium-types = { path = "../prism-lium-types" }
proof-eval = { path = "../proof-eval" }
proof-task = { path = "../proof-task" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tracing = "0.1"

[lints]
workspace = true
